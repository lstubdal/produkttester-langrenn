*[_type == 'test'] | order(name asc){
    name, 
    place,
    date,
    temperature,
    snowdata,
    
    addSkis[]-> {
        number,
        product,
        result,
        ranking
    },

    slug {
        current
    }
}